pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/George-1999-os/alx-backend-python.git'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Running tests..."'
            }
        }
    }
}
